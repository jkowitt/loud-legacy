# OpenAI Configuration (required for AI Prospect Scout feature)
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o-mini

# Legacy CRM API Configuration
NEXT_PUBLIC_API_BASE=http://localhost:8000/api

# If OPENAI_API_KEY is not provided, the app will use fallback prospect suggestions
